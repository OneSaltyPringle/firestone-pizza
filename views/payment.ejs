<h2>Complete Your Payment</h2>
<p>Your total is: $<%= total %></p>
<form method="POST" action="/pizza/complete">
  <input type="hidden" name="total" value="<%= total %>">
  <button class="btn btn-primary">Pay Now (Simulated)</button>
</form>
